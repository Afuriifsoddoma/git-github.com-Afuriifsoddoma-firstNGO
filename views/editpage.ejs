<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Form Example</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
body {
  font-family: 'Roboto', sans-serif;
  margin: 0;
}

.form-container {
  background-color: #5a7d9f; /* Different background color for the form container */
  padding: 20px;
}

.top-right-button {
  position: fixed;
  top: 20px;
  right: 20px;
}
.loading-screen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: #3498db;
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
    }
    a{
        color: #fff;
        font-weight: 400;
    }
    /* Styling for loading animation */
    .loader {
      border: 5px solid #f3f3f3;
      border-top: 5px solid #e39009;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      animation: spin 2s linear infinite;
    }
    body.loaded .loading-screen {
      display: none;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
</style>

</head>
<body>
  <div class="loading-screen">
    <div class="loader"></div>
</div>
  <div class="container-fluid form-container bg-light mt-4 mb-4">
    <form action="/textedit/<%=post.name%>" method="POST"> 
      <div class="form-group">
        <label for="inputText">Input Text:</label>
        <input type="text" class="form-control" id="inputText" placeholder="Enter text" name="name" value="<%=post.name%>">
      </div>
      <div class="form-group">
        <label for="textarea">Textarea:</label>
        <textarea class="form-control" id="textarea" rows="14" placeholder="Enter text"  name="text"><%=post.text%></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Publish</button>
    </form>
  </div>

  <a href="/editposts" class="btn btn-success top-right-button">Back</a>

  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script>
    window.addEventListener("load",()=>{
      setTimeout(function(){
       document.body.classList.add('loaded');
   },2500);
    })
  </script>
</body>
</html>

    