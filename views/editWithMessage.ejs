<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Form Example</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
body {
  font-family: 'Roboto', sans-serif;
  margin: 0;
}

.form-container {
  background-color: #5a7d9f; /* Different background color for the form container */
  padding: 20px;
}

.top-right-button {
  position: fixed;
  top: 20px;
  right: 20px;
}
.success {
      display: block;
      background-color: #007bff;
      color: white;
      padding: 20px;
      border-radius: 5px;
      margin-top: 20px;
      text-align: center;
      animation: slide 1.5s ease-in-out forwards;
    }
    @keyframes slide {
      0% {
        transform: translateY(-50%);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }
</style>

</head>
<body>

  <div class="container-fluid form-container bg-light mt-4 mb-4">
    <form  method="POST"> 
      <div class="form-group">
        <label for="inputText">Input Text:</label>
        <input type="text" class="form-control" id="inputText" placeholder="Enter text" name="name">
      </div>
      <div class="form-group">
        <label for="textarea">Textarea:</label>
        <textarea class="form-control" id="textarea" rows="3" placeholder="Enter text"  name="text"> </textarea>
      </div>
      <button type="submit" class="btn btn-primary">Publish</button>
    </form>
    <div class="success" id="success">
        <p> <%=message%></p>
      </div>
  </div>

  <a href="/editposts" class="btn btn-success top-right-button">Back</a>

  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script>
     window.addEventListener("load",()=>{
        setTimeout(function() {
            document.getElementById('success').style.display = 'none';
            window.location.href="/editposts";
          }, 3000);
     })
  </script>
</body>
</html>

    