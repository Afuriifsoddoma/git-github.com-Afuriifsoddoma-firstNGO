<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User List</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Custom fonts */
    body {
      font-family: Arial, sans-serif;
      background-color: #12a864; /* Body background color */
      transition: background-color 0.3s ease;
    }
    h4, p {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Different font for headings and paragraphs */
      margin: 0;
    }
    /* Additional custom styles */
    .container {
      display: none; /* Initially hide the container */
      background-color: hwb(0 99% 0%); /* Container background color */
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin: 20px;
      transition: box-shadow 0.3s ease;
    }
    .user-info {
      display: flex;
      align-items: center;
      justify-content: space-between;
      border: 1px solid #ccc;
      padding: 10px;
      margin-bottom: 10px;
      transition: border-color 0.3s ease;
    }
    .user-info:hover {
      border-color: #007bff; /* Hover effect for user info */
    }
    .user-info .user-description {
      margin-left: 20px;
    }
    .delete-btn {
      color: red;
      text-decoration: none;
      transition: color 0.3s ease;
    }
    .delete-btn:hover {
      color: darkred; /* Hover effect for delete button */
      text-decoration: underline;
    }
    .loader-wrapper {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background-color: #77c70d;
      z-index: 9999;
    }
    .loader {
      border: 6px solid #f3f3f3;
      border-top: 6px solid #3498db;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      animation: spin 1s linear infinite;
    }
    .top-right {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: #3498db;
       
    }
    .btn-anchor{
        color: #f5d909;
        background-color: rgba(3, 198, 201, 0.85);
        font-size: 20px;
        font-weight: 600;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body>

<div class="loader-wrapper">
  <div class="loader"></div>
</div>

<div class="container mt-4">
  <% array.forEach((arr, index)=>{ %>
    <div class="user-info">
      <div>
        <h4><%=arr.name%></h4>
        <p class="user-description"><%=arr.comment%></p>
      </div>
      <a href="/deleteSpecificComment/<%=arr.comment%>" class="delete-btn">Delete</a>
    </div>
  <% }) %>
</div>
<div class="top-right">
    <a class="btn-anchor" href="/adminLinks">BACK</a>
    <a class="btn-anchor" href="/news">Check</a>
  </div>

<script>
  window.addEventListener('load', function() {
    setTimeout(function(){
        document.querySelector('.loader-wrapper').style.display = 'none';
    document.querySelector('.container').style.display = 'block';
    document.body.style.backgroundColor = '#12a864'; /* Change body background color */
    },3000)
   
  });
</script>

<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
